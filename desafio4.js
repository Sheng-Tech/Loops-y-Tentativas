let numero = prompt ("¡Por favor ingresa un número!"); 
let contador = 0;

while (numero >= contador) { 
    alert (`Cuenta progresiva en: ${contador}`);
    console.log (contador);
    contador++; 
}